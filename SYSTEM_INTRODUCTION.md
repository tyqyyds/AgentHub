# 智维 AgentHub — 系统介绍

## 一、系统概述

**智维 AgentHub** 是一套基于大语言模型多智能体协同的**意图驱动网络自愈与运维系统**。系统以自然语言意图输入为起点，通过多Agent协同编排、ReAct推理引擎、A2A跨域通信等核心技术，实现从意图理解、冲突检测、策略生成、命令执行到结果验证的全链路自动化网络运维闭环。

**核心设计理念：** 自然语言意图输入 → 多Agent协同处理 → 自动化网络运维执行 → SLA持续监控 → 自适应策略重建

**系统定位：** 面向电信运营商、大型企业网络运维场景，将传统人工CLI操作模式升级为AI驱动的意图运维模式，显著降低运维复杂度和人为错误率。

---

## 二、系统架构

### 2.1 整体架构

系统采用**前后端分离 + 多Agent微服务**架构，分为四层：

```
┌─────────────────────────────────────────────────────────────┐
│                     展示层 (Presentation)                     │
│   Vue 3 + Element Plus + ECharts + Leaflet/高德地图           │
│   10个功能页面 + AI助手浮层 + 响应式多端适配                    │
├─────────────────────────────────────────────────────────────┤
│                     网关层 (Gateway)                          │
│   Nginx 反向代理 + Gzip压缩 + 静态资源缓存 + WebSocket代理     │
├─────────────────────────────────────────────────────────────┤
│                     服务层 (Service)                          │
│   FastAPI + 21个API模块 + WebSocket实时通信                    │
│   ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│   │ Agent    │ │ LLM      │ │ Security │ │ Workflow │       │
│   │ Engine   │ │ Gateway  │ │ Chain    │ │ Engine   │       │
│   └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
├─────────────────────────────────────────────────────────────┤
│                     数据层 (Data)                             │
│   PostgreSQL(业务数据) + Redis(缓存/限流) + InfluxDB(遥测)     │
│   SQLite(开发模式) + 文件存储(知识库/自定义工具)                │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 Agent系统架构

系统核心是15个专业Agent，通过A2A通信总线和编排器协同工作：

```
                    ┌─────────────────────┐
                    │   Multi-Agent       │
                    │   Orchestrator      │
                    │ (Supervisor/Parallel│
                    │  /Debate 三种模式)   │
                    └─────────┬───────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
  ┌─────▼──────┐      ┌──────▼──────┐      ┌──────▼──────┐
  │ 意图解析    │      │ 冲突检测    │      │ 策略规划    │
  │ Agent      │      │ Agent      │      │ Agent      │
  │(DeepSeek + │      │(5类冲突规则│      │(华为/Cisco/ │
  │ LangChain) │      │ 检测引擎)  │      │ H3C模板)   │
  └─────┬──────┘      └──────┬──────┘      └──────┬──────┘
        │                     │                     │
  ┌─────▼──────┐      ┌──────▼──────┐      ┌──────▼──────┐
  │ 执行Agent  │      │ 验证Agent   │      │ ReAct推理   │
  │(沙箱检测 + │      │(意图目标    │      │ Engine     │
  │ 设备下发)  │      │  达成验证)  │      │(智谱GLM +  │
  └─────┬──────┘      └──────┬──────┘      │ Function   │
        │                     │             │ Calling)   │
        │              ┌──────▼──────┐      └──────┬──────┘
        │              │ SLA评估     │             │
        │              │ + 策略重建   │             │
        │              └─────────────┘             │
  ┌─────▼──────────────────────────────────────────▼──────┐
  │                    A2A 通信总线                         │
  │          (发布订阅 / 直接消息 / 请求响应)                 │
  └───────────────────────────┬───────────────────────────┘
                              │
  ┌───────────────────────────▼───────────────────────────┐
  │              跨域注册中心 + 语义路由                      │
  │    (Agent发现 / 负载均衡 / 能力匹配 / JSON-RPC 2.0)     │
  └───────────────────────────────────────────────────────┘
```

---

## 三、核心功能模块

### 3.1 意图驱动运维

这是系统的核心功能，实现了从自然语言到网络配置变更的全链路自动化：

| 阶段 | 模块 | 功能描述 |
|------|------|----------|
| 意图输入 | IntentCenter | 自然语言输入，支持拼音+关键词智能补全，5种快速模板 |
| 意图解析 | IntentParserAgent | DeepSeek大模型解析 + LangChain降级 + 规则匹配兜底，支持9种意图类型 |
| 安全扫描 | IntentScanner | 关键词黑名单 + SQL注入 + Shell注入 + 路径遍历检测 |
| 冲突检测 | ConflictDetectorAgent | 5类冲突规则：带宽超额/ACL冲突/QoS优先级/设备状态/跨意图策略 |
| 策略生成 | PolicyPlannerAgent | 华为/Cisco/H3C三厂商配置模板，根据意图类型和设备自动生成CLI命令 |
| 审批流程 | IntentApproval | 两级审批链（operator → admin），支持审批/拒绝/冲突检查 |
| 命令执行 | ExecutionAgent | 沙箱高危命令检测 + SSH/NETCONF设备下发 + Mock开发模式 |
| 结果验证 | VerificationAgent | 检查意图目标达成度，返回completed/partial/failed/monitoring |
| SLA监控 | SLAEvaluator | 30秒周期评估，指标偏离时触发策略重建 |
| 策略重建 | PolicyReconstructor | 根据SLA违规指标自动映射修复动作，生成工具调用计划 |

**支持的9种意图类型：**

| 意图类型 | 说明 | 示例 |
|----------|------|------|
| bandwidth_guarantee | 带宽保障 | "为视频会议保障100M带宽" |
| access_control | 访问控制 | "禁止10.0.1.0/24访问服务器区" |
| qos_policy | QoS策略 | "设置语音流量优先级为高" |
| link_management | 链路管理 | "启用核心交换机备用链路" |
| device_config | 设备配置 | "配置核心路由器OSPF" |
| traffic_shaping | 流量整形 | "限制P2P流量到50Mbps" |
| fault_diagnosis | 故障诊断 | "诊断出口路由器丢包原因" |
| performance_monitoring | 性能监控 | "监控数据中心链路延迟" |
| git_clone_bandwidth | Git克隆带宽 | "为代码仓库克隆保障带宽" |

### 3.2 AI智能助手

全局浮层式AI助手，贯穿所有页面，提供上下文感知的智能交互：

**组件架构：**
- **FloatingBall** — 可拖拽浮动球，5种状态动画（呼吸/思考/执行/告警/拦截）
- **BubbleCard** — 页面上下文感知气泡，根据当前页面提供快捷操作
- **ChatPanel** — 对话面板，支持3种场景模式切换

**三种对话模式：**

| 模式 | 实现 | 特点 |
|------|------|------|
| 普通对话 | AssistantRouterAgent | 规则路由 + LLM增强，支持导航/查询/控制/场景切换 |
| 流式对话 | 智谱GLM SSE | 实时流式输出，支持上下文实体提取 |
| ReAct推理 | ReActEngine | 智谱GLM Function Calling，最多5轮推理，高危操作暂停等审批 |

**12种前端动作类型：**
navigate（路由导航）、fill_form（表单填充）、highlight（高亮实体）、execute（执行命令）、query（查询数据）、scene_mode（场景切换）、qos_config/acl_config/link_config/traffic_config（配置动作）、diagnose（诊断）、monitor（监控）

**三种场景模式：**
- **日常模式** — 正常运维操作
- **应急模式** — 紧急变更，加速审批流程
- **变更冻结** — 拦截所有写操作，仅允许查询

### 3.3 网络拓扑可视化

基于SVG的网络拓扑可视化，支持丰富的交互操作：

- **4种布局算法** — 层次布局、力导向布局、环形布局、网格布局
- **节点交互** — 拖拽、缩放平移（鼠标/触摸/键盘）、搜索定位
- **链路动画** — 粒子流动效果，直观展示数据流向
- **右键菜单** — 设备配置/诊断/SSH/QoS操作
- **A2A消息可视化** — 展示Agent间通信路径
- **小地图** — 全局缩略导航
- **快照管理** — 保存/恢复拓扑布局

### 3.4 故障自愈

事件驱动的自动化故障处理系统：

**三种自愈模式：**

| 模式 | 行为 | 适用场景 |
|------|------|----------|
| 全自动 | 自动检测→分析→执行→验证 | 已知故障类型，低风险修复 |
| 建议模式 | 自动检测→分析→建议→人工确认 | 中风险操作，需人工把关 |
| 仅告警 | 检测→告警→等待人工处理 | 高风险场景，变更冻结期 |

**双视图展示：**
- **时间线视图** — 事件从发现到修复的完整时间线
- **状态机视图** — 事件状态转换可视化（detected → analyzing → healing → completed/failed）

### 3.5 MCP工具管理

MCP（Model Context Protocol）工具管理系统，为AI Agent提供可调用的工具集：

**内置5个核心工具：**

| 工具 | 功能 | 协议 |
|------|------|------|
| show_interface | 查看接口状态 | SSH/NETCONF |
| config_qos | 配置QoS策略 | SSH/NETCONF |
| ping | 连通性测试 | SSH |
| config_acl | 配置ACL规则 | SSH/NETCONF |
| get_device_status | 获取设备状态 | SNMP/SSH |

**扩展能力：**
- 自定义工具添加/删除（含名称唯一性校验、SQL注入检测、敏感词过滤）
- 工具语义搜索（基于描述分词索引）
- 远程MCP服务器管理
- 执行历史记录与回放
- 工具评分与评论

### 3.6 智能体地图

基于高德地图/Leaflet的智能体地理分布可视化：

- **82个全球城市POI** — 数据中心/网络枢纽/智能体节点
- **路径规划** — 支持fastest/balanced/eco三种模式，Haversine球面距离计算
- **跨域路由发现** — 发现远程Agent节点
- **图层切换** — 标准/卫星/交通
- **离线瓦片缓存** — IndexedDB存储，弱网可用
- **自定义标记** — 10种图标/6类分类

### 3.7 运维知识助手

基于关键词匹配的知识问答系统：

- **8篇内置运维文档** — QoS配置、OSPF路由、BGP排查、VLAN配置、防火墙策略、设备巡检、网络自愈、流量工程
- **中文分词** — 2-4字gram + 英文分词混合tokenization
- **置信度显示** — 高/中/低三级
- **文档管理** — 上传/删除/列表分页

### 3.8 工单管理

看板式工单管理系统：

- **5列状态看板** — 待审批/已批准/执行中/已完成/已拒绝
- **两级审批链** — operator审批 → admin审批（admin可跳过后续步骤）
- **4种优先级** — critical/high/medium/low
- **数据导出** — 支持看板汇总数据导出

### 3.9 审计日志

全操作留痕的安全审计系统：

- **多维筛选** — 类型/状态/安全级别/来源/时间范围
- **AI对话记录** — 查看AI交互的完整对话
- **5种专用记录方法** — 意图提交/配置下发/自愈执行/配置回滚/安全告警
- **导出** — JSON/CSV格式
- **实时轮询** — 10秒间隔自动刷新

### 3.10 指挥舱仪表盘

全局概览仪表盘，一屏掌握系统运行状态：

- **4项关键指标** — 活跃意图数、在线设备数、自愈事件数、待审批项
- **设备健康状态** — 列表展示，颜色标识
- **最近意图列表** — 快速查看最新操作
- **限流统计图表** — 可视化展示请求速率

---

## 四、技术架构

### 4.1 技术栈

| 层次 | 技术 | 版本/说明 |
|------|------|-----------|
| **前端框架** | Vue 3 | Composition API + Script Setup |
| **UI组件库** | Element Plus | 2.6+ |
| **状态管理** | Pinia | 2.1+ |
| **路由** | Vue Router | 4.3+ |
| **图表** | ECharts | 5.5+ |
| **地图** | Leaflet + 高德地图JSAPI | 双引擎切换 |
| **构建工具** | Vite 5 | esbuild压缩，代码分割 |
| **类型检查** | TypeScript | 5.3+ |
| **后端框架** | FastAPI | 0.100+，异步ASGI |
| **ORM** | SQLAlchemy 2.0 | 异步模式 + asyncpg |
| **数据验证** | Pydantic v2 | 数据模型 + Settings |
| **LLM-1** | DeepSeek | deepseek-chat，意图解析/对话 |
| **LLM-2** | 智谱GLM | glm-4-flash，Function Calling/流式对话 |
| **LLM编排** | LangChain + LangGraph | Agent工作流编排 |
| **数据库** | PostgreSQL | 生产环境 |
| **缓存** | Redis | 速率限制/会话缓存 |
| **时序库** | InfluxDB | 遥测数据存储 |
| **Web服务器** | Nginx | 反向代理/静态资源/Gzip |
| **进程管理** | systemd | 后端服务守护 |
| **设备协议** | NETCONF (ncclient) + SSH (paramiko) | 网络设备管理 |

### 4.2 安全架构

系统实现了6层纵深安全防护：

```
用户输入
   │
   ▼
┌──────────────────┐
│ 1. Prompt注入防护  │  58种恶意模式检测（忽略指令/角色扮演/SQL注入/XSS/路径遍历）
│ PromptGuard      │  输入归一化（HTML/URL/十六进制/Unicode解码）
└────────┬─────────┘
         │
┌────────▼─────────┐
│ 2. 意图安全扫描    │  关键词黑名单 + SQL注入 + Shell注入 + 路径遍历
│ IntentScanner    │  递归扫描结构化参数，风险等级：safe/medium/high
└────────┬─────────┘
         │
┌────────▼─────────┐
│ 3. 命令沙箱检测    │  19条危险命令规则 + 31条恶意模式 + 37条中文危险关键词
│ SecuritySandbox  │  10条混淆检测规则，高危命令自动拦截
└────────┬─────────┘
         │
┌────────▼─────────┐
│ 4. 变更窗口管理    │  可配置允许变更的时间段，紧急变更始终允许
│ ChangeWindow     │  变更冻结模式下拦截所有写操作
└────────┬─────────┘
         │
┌────────▼─────────┐
│ 5. 配置回滚       │  执行前自动备份，失败或健康检查不通过时自动回滚
│ ConfigRollback   │  支持手动回滚到任意历史备份点
└────────┬─────────┘
         │
┌────────▼─────────┐
│ 6. 速率限制       │  基于角色的滑动窗口限流 + 自适应阈值调整
│ RateLimiter      │  优先级队列（critical/high/medium/low）
└──────────────────┘
```

### 4.3 RBAC权限模型

| 角色 | 权限范围 | 可访问页面 |
|------|----------|-----------|
| **admin** | 全部权限（intents/events/agents/audit/users/system/workflow） | 全部10个页面 |
| **operator** | intents读写/events读写/audit读/workflow读写 | 8个页面（排除审计日志） |
| **viewer** | intents读/events读/workflow读 | 4个页面（指挥舱/拓扑/知识/登录） |

**双重权限控制：**
- 路由层：Vue Router beforeEach守卫，无权限重定向
- 组件层：菜单项过滤 + AI助手快捷操作RBAC过滤

### 4.4 实时通信

基于WebSocket的全双工实时通信：

**10种消息类型：**

| 类型 | 方向 | 用途 |
|------|------|------|
| connection_established | 服务端→客户端 | 连接确认，返回用户信息 |
| message | 双向 | 通用消息 |
| notification | 服务端→客户端 | 系统通知 |
| status_update | 服务端→客户端 | 状态变更推送 |
| topology_update | 服务端→客户端 | 拓扑全量刷新 |
| device_update | 服务端→客户端 | 设备增量更新 |
| alert | 服务端→客户端 | 告警通知 |
| intent_update | 服务端→客户端 | 意图状态变更 |
| assistant_action | 服务端→客户端 | AI助手动作指令 |
| ping/pong | 双向 | 心跳保活（30秒） |

**连接管理：**
- JWT token认证，认证失败(4001/4003)停止重连
- 自动重连，最多5次，递增延迟（3s/6s/9s/12s/15s）
- 单例模式共享连接，引用计数管理生命周期

### 4.5 LLM网关

统一LLM调用网关，管理DeepSeek和智谱GLM双提供商：

```
                    ┌──────────────┐
                    │  LLM Gateway │
                    └──────┬───────┘
                           │
              ┌────────────┼────────────┐
              │            │            │
     ┌────────▼───┐  ┌────▼─────┐  ┌──▼──────────┐
     │ DeepSeek   │  │ 智谱GLM  │  │ 故障转移    │
     │ 意图解析   │  │ Function │  │ 自动切换    │
     │ 对话增强   │  │ Calling  │  │ 提供商      │
     │ 配置生成   │  │ 流式对话 │  │             │
     └────────────┘  └──────────┘  └─────────────┘
```

**按任务类型自动选择提供商：**

| 任务类型 | 首选提供商 | 降级方案 |
|----------|-----------|----------|
| INTENT_PARSE | 智谱GLM | DeepSeek → 规则匹配 |
| COPILOT_CHAT | DeepSeek | 智谱GLM |
| COPILOT_STREAM | 智谱GLM SSE | DeepSeek |
| CONFIG_GENERATE | DeepSeek | 智谱GLM |
| FAULT_DIAGNOSE | DeepSeek | 智谱GLM |

### 4.6 数据模型

系统共8个核心数据表：

| 模型 | 表名 | 核心字段 | 用途 |
|------|------|----------|------|
| User | users | username, role, password_hash | 用户认证与权限 |
| Intent | intents | intent_name, structured_params(JSON), approval_status, sla_conditions(JSON) | 意图生命周期管理 |
| SelfHealingEvent | self_healing_events | event_type, severity, healing_mode, status | 故障自愈事件 |
| PolicyTemplate | policy_templates | vendor, device_type, function, template_content | 策略配置模板 |
| AgentRegistry | agent_registry | agent_id, domain, capabilities(JSON), status | Agent注册信息 |
| Device | devices | device_type, vendor, ip_address, status, cpu/memory_usage | 网络设备信息 |
| DeviceLink | device_links | source_device_id, target_device_id, bandwidth, current_load | 设备链路关系 |
| AuditLog | audit_logs | action, target_device, commands(JSON), security_type | 操作审计日志 |
| MCPTool | mcp_tools | name, input_schema(JSON), output_schema(JSON), endpoint | MCP工具定义 |

---

## 五、核心业务流程

### 5.1 意图执行主流程

```
用户输入自然语言
       │
       ▼
┌──────────────┐    ┌──────────────────────────────────┐
│ 前端意图解析  │───→│ intentParser.ts (9种模式匹配)      │
│ (即时反馈)    │    │ 置信度评估 + 输入验证              │
└──────┬───────┘    └──────────────────────────────────┘
       │
       ▼
┌──────────────┐    ┌──────────────────────────────────┐
│ 安全扫描      │───→│ IntentScanner: SQL/Shell/路径遍历  │
│              │    │ PromptGuard: 58种注入模式          │
└──────┬───────┘    └──────────────────────────────────┘
       │
       ▼
┌──────────────┐    ┌──────────────────────────────────┐
│ AI增强解析    │───→│ DeepSeek/智谱GLM 结构化输出        │
│              │    │ 降级: LangChain → 规则匹配          │
└──────┬───────┘    └──────────────────────────────────┘
       │
       ▼
┌──────────────┐    ┌──────────────────────────────────┐
│ 冲突检测      │───→│ 5类规则引擎并行检测                │
│              │    │ 带宽/ACL/QoS/设备/跨意图           │
└──────┬───────┘    └──────────────────────────────────┘
       │
       ├── 有冲突 → 终止，返回冲突建议
       │
       ▼ 无冲突
┌──────────────┐    ┌──────────────────────────────────┐
│ 审批流程      │───→│ operator审批 → admin审批           │
│              │    │ admin可跳过后续步骤                 │
└──────┬───────┘    └──────────────────────────────────┘
       │
       ▼ 审批通过
┌──────────────┐    ┌──────────────────────────────────┐
│ 策略生成      │───→│ 华为/Cisco/H3C 三厂商模板匹配      │
│              │    │ 子网关键词定位目标设备              │
└──────┬───────┘    └──────────────────────────────────┘
       │
       ▼
┌──────────────┐    ┌──────────────────────────────────┐
│ 沙箱检测      │───→│ 19条危险命令 + 31条恶意模式        │
│              │    │ 高危命令自动拦截                    │
└──────┬───────┘    └──────────────────────────────────┘
       │
       ▼ 安全通过
┌──────────────┐    ┌──────────────────────────────────┐
│ 配置备份      │───→│ ConfigRollbackManager 自动备份     │
│              │    │ 执行失败可自动回滚                  │
└──────┬───────┘    └──────────────────────────────────┘
       │
       ▼
┌──────────────┐    ┌──────────────────────────────────┐
│ 命令执行      │───→│ SSH/NETCONF 下发到网络设备         │
│              │    │ Mock模式用于开发测试                │
└──────┬───────┘    └──────────────────────────────────┘
       │
       ▼
┌──────────────┐    ┌──────────────────────────────────┐
│ 结果验证      │───→│ 检查意图目标达成度                 │
│              │    │ completed/partial/failed/monitoring│
└──────┬───────┘    └──────────────────────────────────┘
       │
       ├── completed → 结束
       ├── partial → 策略重建 → 重新执行
       ├── failed → 自动回滚
       └── monitoring → 进入SLA持续监控
              │
              ▼
┌──────────────┐    ┌──────────────────────────────────┐
│ SLA持续监控   │───→│ 30秒周期评估意图SLA条件            │
│              │    │ 指标偏离时触发策略重建               │
└──────┬───────┘    └──────────────────────────────────┘
       │
       ▼ SLA偏离
┌──────────────┐    ┌──────────────────────────────────┐
│ 策略重建      │───→│ 违规指标 → 修复动作映射            │
│              │    │ bandwidth→qos_adjustment           │
│              │    │ latency→route_optimization         │
│              │    │ packet_loss→interface_failover     │
│              │    │ cpu→load_balancing                 │
│              │    │ memory→resource_optimization       │
└──────────────┘    └──────────────────────────────────┘
```

### 5.2 ReAct推理流程

AI助手的核心推理引擎，实现思考-行动-观察的循环推理：

```
用户输入问题
       │
       ▼
┌──────────────────┐
│ Round 1: Reasoning │  智谱GLM分析问题，决定使用哪些工具
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Tool Calling      │  Function Calling选择工具
└────────┬─────────┘
         │
    ┌────┴────┐
    │         │
  低危工具   高危工具
    │         │
    ▼         ▼
 直接执行   暂停等待审批
    │         │
    │     ┌───┴───┐
    │     │       │
    │    批准    拒绝
    │     │       │
    │     ▼       ▼
    │   继续执行  终止推理
    │     │
    ▼     ▼
┌──────────────────┐
│ Observation       │  观察工具执行结果
└────────┬─────────┘
         │
    ┌────┴────┐
    │         │
  需要更多   推理完成
  信息       │
    │         │
    ▼         ▼
  回到Round N  输出最终答案
  (最多5轮)    + 工作流时间线
```

### 5.3 多Agent协作流程

编排器支持三种协作模式：

**1. 主管模式 (Supervisor)**
```
Orchestrator ──→ Agent-A ──→ Agent-B ──→ Agent-C
   (串行)         依赖A完成    依赖B完成
```
按依赖关系串行执行，适合有严格顺序要求的任务。

**2. 并行模式 (Parallel)**
```
Orchestrator ──┬──→ Agent-A
               ├──→ Agent-B    ──→ 汇总结果
               └──→ Agent-C
```
asyncio.gather并行执行，适合无依赖关系的独立子任务。

**3. 辩论模式 (Debate)**
```
Round 1: Agent-A方案 ←→ Agent-B方案
Round 2: 互相质疑，修正方案
Round 3: 寻找共识，输出最终方案
```
3轮辩论，适合需要多角度分析的高风险决策。

### 5.4 跨域Agent通信流程

```
┌─────────────┐                          ┌─────────────┐
│  本地Agent   │                          │  远程Agent   │
│  (AgentHub)  │                          │  (Edge)     │
└──────┬──────┘                          └──────┬──────┘
       │                                        │
       │  1. Agent注册 (agent.register)          │
       │────────────────────────────────────────→│
       │                                        │
       │  2. 心跳保活 (agent.heartbeat)           │
       │←───────────── 30s ─────────────────────→│
       │                                        │
       │  3. 能力发现 (agent.discover)            │
       │────────────────────────────────────────→│
       │  返回: 能力列表 + 负载信息                │
       │←────────────────────────────────────────│
       │                                        │
       │  4. 语义路由                            │
       │  关键词提取 → 能力匹配(0.7权重)           │
       │           + 负载评分(0.3权重)            │
       │           → 选择最优Agent               │
       │                                        │
       │  5. 调用Agent (agent.call)              │
       │  JSON-RPC 2.0 + JWT认证                │
       │────────────────────────────────────────→│
       │  返回: 执行结果                          │
       │←────────────────────────────────────────│
```

---

## 六、部署架构

### 6.1 生产环境部署

```
                    ┌─────────────┐
                    │   Client    │
                    │  (Browser)  │
                    └──────┬──────┘
                           │ HTTP/WS
                           ▼
                    ┌─────────────┐
                    │   Nginx     │
                    │   :80       │
                    │             │
                    │ / → dist/   │
                    │ /api → :8000│
                    │ /ws → :8000 │
                    └──────┬──────┘
                           │
              ┌────────────┼────────────┐
              │            │            │
       ┌──────▼──────┐     │     ┌──────▼──────┐
       │  Frontend   │     │     │  Backend    │
       │  (dist/)    │     │     │  (uvicorn)  │
       │  静态资源    │     │     │  :8000      │
       └─────────────┘     │     └──────┬──────┘
                           │            │
                           │     ┌──────▼──────┐
                           │     │ PostgreSQL  │
                           │     │ :5432       │
                           │     └─────────────┘
                           │     ┌─────────────┐
                           │     │   Redis     │
                           │     │ :6379       │
                           │     └─────────────┘
                           │     ┌─────────────┐
                           └────→│  InfluxDB   │
                                 │ :8086       │
                                 └─────────────┘
```

### 6.2 服务管理

| 服务 | 管理方式 | 配置文件 |
|------|----------|----------|
| Backend API | systemd (agenthub-backend) | /etc/systemd/system/agenthub-backend.service |
| Nginx | systemd | /etc/nginx/sites-available/agenthub |
| PostgreSQL | systemd | 默认配置 |
| Redis | systemd | 默认配置 |

### 6.3 前端构建优化

| 优化项 | 策略 |
|--------|------|
| 代码分割 | vue-vendor / ui-vendor / charts 三组手动分块 |
| 路由懒加载 | 全部10个页面组件动态import |
| 压缩 | esbuild minify，sourcemap关闭 |
| 缓存 | /assets/ 目录30天强缓存，immutable |
| Gzip | Nginx开启，最小256字节 |

---

## 七、系统特色

### 7.1 意图驱动

区别于传统CLI/GUI运维方式，用户只需用自然语言描述运维需求，系统自动完成从理解到执行的完整链路。支持9种意图类型，覆盖网络运维主要场景。

### 7.2 多Agent协同

15个专业Agent通过A2A通信总线协同工作，支持主管/并行/辩论三种协作模式。每个Agent职责单一、可独立演进，通过编排器灵活组合应对复杂运维场景。

### 7.3 ReAct推理

AI助手采用ReAct（Reasoning + Acting）推理引擎，实现思考-行动-观察的循环推理。结合智谱GLM的Function Calling能力，可自主选择并调用12+种工具，高危操作自动暂停等待人工审批。

### 7.4 纵深安全

6层安全防护链（Prompt注入防护 → 意图安全扫描 → 命令沙箱检测 → 变更窗口管理 → 配置回滚 → 速率限制），确保AI驱动的自动化操作在安全可控范围内执行。

### 7.5 SLA闭环

意图执行后进入SLA持续监控，30秒周期评估。指标偏离时自动触发策略重建，形成"执行-监控-调整"的闭环，确保运维意图持续达成。

### 7.6 跨域协作

基于A2A协议和语义路由，支持跨域Agent发现与协作。本地负载过高时自动卸载任务到远程Agent，实现算力的弹性调度。

### 7.7 全局AI助手

浮动球式AI助手贯穿所有页面，上下文感知提供快捷操作。支持3种对话模式和3种场景模式，从对话到页面操作的完整闭环。

---

## 八、API总览

系统提供21个API模块，覆盖完整的运维场景：

| 模块 | 前缀 | 核心端点数 | 功能 |
|------|------|-----------|------|
| 认证授权 | /api/v1/auth | 5 | 登录/注册/刷新/用户信息/初始化 |
| 意图管理 | /api/v1/intents | 7 | CRUD/审批/拒绝/执行/冲突检查 |
| 事件告警 | /api/v1/events | 3 | 列表/创建/回滚 |
| Agent管理 | /api/v1/agents | 3 | 列表/创建/详情 |
| 策略模板 | /api/v1/templates | 3 | 列表/创建/详情 |
| 审计日志 | /api/v1/audit | 4 | 列表/创建/计数/详情 |
| 验证 | /api/v1/validation | 4 | 验证/预演/假设分析/回滚生成 |
| 系统熔断 | /api/v1/system | 3 | 熔断/状态/健康 |
| 分布式锁 | /api/v1/locks | 5 | 加锁/解锁/状态/列表/续期 |
| 配置提交 | /api/v1/commit | 4 | 提交/确认/取消/待提交 |
| MCP工具 | /api/v1/mcp-tools | 8 | 列表/搜索/执行/自定义/远程/历史 |
| 速率限制 | /api/v1/rate-limit | 1 | 状态查询 |
| 网络拓扑 | /api/v1/topology | 2 | 拓扑数据/设备详情 |
| 跨域管理 | /api/v1/cross-domain | 5 | 注册/发现/路由/A2A/拓扑 |
| 遥测数据 | /api/v1/telemetry | 4 | 设备指标/历史/SLA状态/评估 |
| 知识问答 | /api/v1/knowledge | 4 | 查询/文档CRUD |
| 工单管理 | /api/v1/workflow | 6 | 创建/审批/拒绝/执行/完成/看板 |
| 算力调度 | /api/v1/compute | 3 | 提交任务/任务列表/节点列表 |
| 地图服务 | /api/v1/map | 8 | POI/路径/图层/标记/Agent分布 |
| AI助手 | /api/v1/assistant | 7 | 对话/流式/ReAct/审批/上下文/历史/状态 |
| V2增强 | /api/v2/* | 16 | 一站式执行/副驾/安全扫描/A2A等 |
| 健康检查 | /api/health | 1 | 服务健康状态 |
| WebSocket | /ws | 1 | 实时双向通信 |

**总计：90+ API端点**

---

## 九、数据流总览

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户交互层                                │
│  自然语言输入 / 页面操作 / AI助手对话 / 地图交互 / 工单审批       │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                        API网关层                                │
│  Nginx(80) → FastAPI(8000) + WebSocket(/ws)                    │
│  CORS + RateLimit + JWT认证                                     │
└───────────────────────────┬─────────────────────────────────────┘
                            │
              ┌─────────────┼─────────────┐
              │             │             │
              ▼             ▼             ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  意图处理流   │  │  AI推理流    │  │  数据查询流   │
│  解析→扫描→  │  │  ReAct循环→  │  │  拓扑/设备/  │
│  冲突→审批→  │  │  Function    │  │  遥测/审计/  │
│  生成→执行→  │  │  Calling→    │  │  事件/工单   │
│  验证→SLA    │  │  工具执行    │  │              │
└──────┬───────┘  └──────┬───────┘  └──────┬───────┘
       │                 │                 │
       └─────────────┬───┘─────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                        Agent协同层                               │
│  A2A Bus(发布订阅) + Orchestrator(编排) + Registry(注册发现)     │
│  Semantic Router(语义路由) + Tool Registry(工具注册)             │
└───────────────────────────┬─────────────────────────────────────┘
                            │
              ┌─────────────┼─────────────┐
              │             │             │
              ▼             ▼             ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  LLM服务层   │  │  设备交互层   │  │  数据持久层   │
│  DeepSeek    │  │  SSH/NETCONF │  │  PostgreSQL  │
│  智谱GLM     │  │  DevicePool  │  │  Redis       │
│  LangChain   │  │  EdgeAgent   │  │  InfluxDB    │
│  故障转移    │  │  Mock模式    │  │  SQLite      │
└──────────────┘  └──────────────┘  └──────────────┘
```

---

## 十、运行环境

| 项目 | 要求 |
|------|------|
| 操作系统 | Ubuntu 20.04+ / CentOS 7+ |
| Python | 3.10+ |
| Node.js | 18+ |
| PostgreSQL | 15+ |
| Redis | 7+ |
| Nginx | 1.20+ |
| 最低配置 | 2核4G |
| 推荐配置 | 4核8G+ |
| 磁盘空间 | 10G+（不含数据库） |

---

*文档版本：1.0.0 | 生成日期：2026-05-30*
