# 智维 AgentHub 前端页面设计规范

> **版本**：v1.0 · **生效日期**：2026-06-13 · **适用范围**：智维 AgentHub 前端全部 Vue3 页面
> **Token 定义文件**：`frontend/src/tokens.css`
> **审计基线**：`前端页面设计规范检查.md`（约 1202 处违规，合规率约 40%）

---

## 目录

1. [总则](#1-总则)
2. [色彩系统](#2-色彩系统)
3. [排版规则](#3-排版规则)
4. [间距系统](#4-间距系统)
5. [圆角与阴影](#5-圆角与阴影)
6. [页面布局](#6-页面布局)
7. [组件样式](#7-组件样式)
8. [交互反馈](#8-交互反馈)
9. [响应式设计](#9-响应式设计)
10. [无障碍合规](#10-无障碍合规)
11. [验收标准](#11-验收标准)
12. [附录：硬编码迁移映射表](#12-附录硬编码迁移映射表)

---

## 1. 总则

### 1.1 目标

本规范旨在建立统一、可执行的前端视觉与交互标准，确保智维 AgentHub 全部页面在视觉一致性、代码可维护性、无障碍合规性方面达到专业水准。

### 1.2 核心原则

| 原则 | 说明 |
|------|------|
| **Token 优先** | 所有颜色、间距、圆角、阴影、字体必须使用 `var(--xxx)` Token，禁止硬编码 |
| **OKLCH 色彩** | 所有颜色值必须使用 OKLCH 色彩空间，禁止 hex/rgb/hsl 硬编码 |
| **语义化命名** | Token 命名必须体现语义用途（如 `--color-text-primary`），而非具体数值 |
| **渐进增强** | 优先保证核心功能可用，再增强视觉效果 |
| **无障碍优先** | 所有交互元素必须可通过键盘访问，必须适配高对比度和减少动画偏好 |

### 1.3 Token 体系架构

```
tokens.css（:root 全局定义）
  ├── 色彩系统（OKLCH + RGB 分解变量）
  ├── 间距系统（2px 基准递增）
  ├── 圆角系统（0-9999px）
  ├── 阴影系统（xs-xl + glow）
  ├── 排版系统（字体族/字号/字重/行高/字间距）
  ├── 布局尺寸（侧边栏/顶栏/内容区/卡片）
  ├── 过渡动画（时长/缓动曲线）
  ├── 层级（z-index）
  └── 断点（供 JS 读取）
```

### 1.4 禁止事项

| 禁止项 | 正确做法 | 示例 |
|--------|---------|------|
| 硬编码颜色值 | 使用 `var(--color-xxx)` | ❌ `#165DFF` → ✅ `var(--color-primary)` |
| 硬编码 rgba() | 使用 RGB 分解变量 | ❌ `rgba(22,93,255,0.2)` → ✅ `rgba(var(--color-primary-rgb), 0.2)` |
| 硬编码间距 px | 使用 `var(--spacing-xxx)` | ❌ `padding: 12px` → ✅ `padding: var(--spacing-md)` |
| 硬编码圆角 px | 使用 `var(--radius-xxx)` | ❌ `border-radius: 8px` → ✅ `border-radius: var(--radius-lg)` |
| 硬编码字重数字 | 使用 `var(--font-weight-xxx)` | ❌ `font-weight: 600` → ✅ `font-weight: var(--font-weight-semibold)` |
| 硬编码字号 px | 使用 `var(--font-size-xxx)` | ❌ `font-size: 14px` → ✅ `font-size: var(--font-size-base)` |
| 硬编码字体族 | 使用 `var(--font-family-xxx)` | ❌ `monospace` → ✅ `var(--font-family-mono)` |
| hex/rgb/hsl 颜色值 | 使用 OKLCH Token | ❌ `#EF4444` → ✅ `var(--color-error)` |

---

## 2. 色彩系统

### 2.1 色彩空间规范

**强制**：所有颜色值必须使用 OKLCH 色彩空间。

OKLCH 优势：
- 感知均匀性：相同亮度差值在视觉上产生一致的变化
- 色域更广：支持 P3 等广色域显示器
- 可预测性：调整 L 通道即可均匀调亮/调暗

```
格式：oklch(L C H)
L = 亮度 0-1    （0=纯黑，1=纯白）
C = 色度 0-0.4  （0=灰色，0.4=高饱和）
H = 色相 0-360  （0/360=红，120=绿，240=蓝）
```

### 2.2 品牌主色

| Token | OKLCH 值 | 用途 | 应用场景 |
|-------|---------|------|---------|
| `--color-primary` | `oklch(0.55 0.22 264)` | 品牌主色 | 主按钮、选中态、关键链接、进度条 |
| `--color-primary-light` | `oklch(0.68 0.18 264)` | 主色浅色 | 次级按钮、标签背景、悬停态 |
| `--color-primary-dark` | `oklch(0.42 0.20 264)` | 主色深色 | 按钮按下态、深色强调 |
| `--color-cosmic` | `oklch(0.58 0.22 255)` | 宇宙蓝 | 数据可视化、科技感强调、渐变终点 |

**应用规则**：
- 每个页面最多使用 2 种品牌色作为视觉焦点
- 主色占比不超过页面面积的 10%（避免视觉疲劳）
- 主色与背景色亮度差 ≥ 0.35（确保可读性）

### 2.3 语义色

| Token | OKLCH 值 | 用途 | 应用场景 |
|-------|---------|------|---------|
| `--color-success` | `oklch(0.65 0.18 145)` | 成功/正常 | 在线状态、操作成功提示、健康指标 |
| `--color-warning` | `oklch(0.75 0.16 75)` | 警告/注意 | 告警提示、即将到期、资源紧张 |
| `--color-error` | `oklch(0.60 0.22 25)` | 错误/危险 | 错误提示、离线状态、危险操作 |
| `--color-info` | `oklch(0.62 0.16 240)` | 信息/提示 | 帮助提示、信息说明、引导文字 |

**应用规则**：
- 语义色仅用于传达状态含义，不作为装饰色
- 同一状态含义必须使用统一语义色（不可用主色替代成功色）
- 语义色需搭配文字标签，不可仅依赖颜色传达信息

### 2.4 中性色

| Token | 用途 | 应用场景 |
|-------|------|---------|
| `--color-text-primary` | 主文字 | 标题、正文、重要信息 |
| `--color-text-secondary` | 次要文字 | 描述、辅助说明、时间戳 |
| `--color-text-tertiary` | 辅助文字 | 占位符、禁用态文字、提示文字 |
| `--color-text-disabled` | 禁用文字 | 不可操作项文字 |
| `--color-bg-base` | 页面底色 | 全局背景 |
| `--color-bg-elevated` | 卡片/面板 | 内容容器背景 |
| `--color-bg-overlay` | 浮层/弹窗 | 模态框、下拉面板 |
| `--color-bg-hover` | 悬停背景 | 行悬停、按钮悬停 |
| `--color-bg-active` | 激活背景 | 按下态、选中态 |
| `--color-border` | 默认边框 | 卡片边框、分割线 |
| `--color-border-light` | 浅色边框 | 内部分割线、分组边框 |
| `--color-border-focus` | 焦点边框 | 输入框聚焦、Tab 焦点 |

### 2.5 透明度用法

需要透明度的颜色必须使用 RGB 分解变量：

```css
/* 正确 */
background: rgba(var(--color-primary-rgb), 0.15);
border: 1px solid rgba(var(--color-white-rgb), 0.1);

/* 错误 — 禁止硬编码 rgba */
background: rgba(22, 93, 255, 0.15);
```

**高频透明度场景**：

| 场景 | 推荐透明度 | 示例 |
|------|-----------|------|
| 悬浮层背景 | 0.85-0.95 | `rgba(var(--color-bg-deep-rgb), 0.9)` |
| 标签/徽章背景 | 0.10-0.20 | `rgba(var(--color-primary-rgb), 0.15)` |
| 边框微光 | 0.05-0.15 | `rgba(var(--color-white-rgb), 0.1)` |
| 遮罩层 | 0.50-0.70 | `rgba(var(--color-black-rgb), 0.6)` |
| 文字次要 | 0.60-0.80 | `rgba(var(--color-white-rgb), 0.7)` |

### 2.6 渐变

| Token | 用途 | 应用场景 |
|-------|------|---------|
| `--gradient-primary` | 品牌渐变 | 主按钮、Hero 区域、重要卡片 |
| `--gradient-cosmic` | 科技渐变 | 数据大屏、可视化面板、科技感区域 |
| `--gradient-dark` | 暗色渐变 | 页面底部、深色区域过渡 |

---

## 3. 排版规则

### 3.1 字体族

| Token | 值 | 应用场景 |
|-------|---|---------|
| `--font-family-base` | Inter, PingFang SC, Microsoft YaHei, system-ui | 全局默认、正文、UI 文字 |
| `--font-family-mono` | JetBrains Mono, Fira Code, Cascadia Code | 代码块、日志输出、数据指标、终端 |
| `--font-family-display` | Inter, PingFang SC, system-ui | 大标题、数字展示、仪表盘指标 |

**规则**：
- 正文必须使用 `var(--font-family-base)`
- 代码/数据/日志必须使用 `var(--font-family-mono)`
- 禁止直接写 `monospace`、`sans-serif`、`serif`

### 3.2 字号层级

| Token | 值 | 用途 | 应用场景 |
|-------|---|------|---------|
| `--font-size-2xs` | 10px | 微型标注 | 角标、状态标签、时间戳 |
| `--font-size-xs` | 11px | 辅助文字 | 副标签、工具提示、表单提示 |
| `--font-size-sm` | 12px | 小号正文 | 表格内容、列表项、侧边栏菜单 |
| `--font-size-base` | 14px | 正文 | 段落、表单标签、按钮文字 |
| `--font-size-md` | 15px | 中号正文 | 重要段落、卡片标题 |
| `--font-size-lg` | 16px | 小标题 | 区块标题、面板标题 |
| `--font-size-xl` | 18px | 标题 | 页面二级标题 |
| `--font-size-2xl` | 20px | 大标题 | 页面一级标题 |
| `--font-size-3xl` | 24px | 特大标题 | 大屏指标、空状态图标旁文字 |
| `--font-size-4xl` | 32px | 超大标题 | 大屏主指标、Hero 标题 |
| `--font-size-5xl` | 36px | 展示级 | 空状态图标、大屏核心数据 |
| `--font-size-6xl` | 48px | 巨幅展示 | 大屏主数字、404/空状态 |

**规则**：
- 同一页面字号层级不超过 4 级
- 相邻层级差至少 2px（如 12px→14px，不可 13px→14px）
- 禁止使用非 Token 字号（如 9px、13px、22px 等不在 Token 表中的值）

### 3.3 字重

| Token | 值 | 用途 | 应用场景 |
|-------|---|------|---------|
| `--font-weight-regular` | 400 | 正文 | 段落、描述、普通文字 |
| `--font-weight-medium` | 500 | 强调正文 | 列表标题、表单标签、导航项 |
| `--font-weight-semibold` | 600 | 小标题 | 卡片标题、面板标题、Tab 激活态 |
| `--font-weight-bold` | 700 | 标题 | 页面标题、区块标题、重要数字 |
| `--font-weight-extrabold` | 800 | 超级强调 | 大屏核心指标、Hero 标题 |

**规则**：
- 正文默认 `var(--font-weight-regular)`，不可使用 500+
- 同一页面字重层级不超过 3 级
- 禁止直接写数字字重（如 `font-weight: 600`）

### 3.4 行高

| Token | 值 | 用途 |
|-------|---|------|
| `--line-height-tight` | 1.2 | 标题、数字指标 |
| `--line-height-snug` | 1.35 | 小标题、列表项 |
| `--line-height-normal` | 1.5 | 正文段落 |
| `--line-height-relaxed` | 1.65 | 长文阅读 |
| `--line-height-loose` | 1.8 | 多行代码块 |

### 3.5 字间距

| Token | 值 | 用途 |
|-------|---|------|
| `--letter-spacing-tight` | -0.02em | 大标题 |
| `--letter-spacing-normal` | 0 | 正文 |
| `--letter-spacing-wide` | 0.02em | 大写标签 |
| `--letter-spacing-wider` | 0.05em | 全大写标题 |

---

## 4. 间距系统

### 4.1 间距 Token

| Token | 值 | 用途 | 应用场景 |
|-------|---|------|---------|
| `--spacing-3xs` | 2px | 微间距 | 图标与文字间隙、边框内距 |
| `--spacing-2xs` | 4px | 极小间距 | 紧凑元素内距、徽章内距 |
| `--spacing-xs` | 6px | 小间距 | 小按钮内距、标签内距 |
| `--spacing-sm` | 8px | 标准小间距 | 输入框内距、列表项间距 |
| `--spacing-sm-md` | 10px | 中小间距 | 按钮内距、表单项间距 |
| `--spacing-md` | 12px | 标准间距 | 卡片内距、面板内距 |
| `--spacing-md-lg` | 14px | 中等间距 | 区块内距 |
| `--spacing-lg` | 16px | 大间距 | 卡片内距、区块间距 |
| `--spacing-xl` | 20px | 较大间距 | 区块间距、段落间距 |
| `--spacing-2xl` | 24px | 大间距 | 页面边距、大区块间距 |
| `--spacing-3xl` | 32px | 超大间距 | 页面区域间距 |
| `--spacing-4xl` | 40px | 巨大间距 | 页面顶部/底部留白 |
| `--spacing-5xl` | 48px | 展示级间距 | Hero 区域间距 |
| `--spacing-6xl` | 64px | 最大间距 | 大屏区域间距 |

### 4.2 间距使用规则

**4 的倍数原则**：优先使用 4 的倍数间距（4/8/12/16/20/24/32），仅在特殊紧凑场景使用 2/6/10/14。

| 场景 | 推荐 Token | 说明 |
|------|-----------|------|
| 组件内部间距 | `--spacing-sm` ~ `--spacing-md` | 8-12px |
| 组件之间间距 | `--spacing-md` ~ `--spacing-lg` | 12-16px |
| 区块之间间距 | `--spacing-xl` ~ `--spacing-2xl` | 20-24px |
| 页面边距 | `--spacing-2xl` ~ `--spacing-3xl` | 24-32px |
| 大屏区域间距 | `--spacing-3xl` ~ `--spacing-6xl` | 32-64px |

**规则**：
- 禁止使用非 Token 间距值（如 3px、5px、7px、9px、15px、18px 等）
- margin 和 padding 必须使用 Token
- gap 必须使用 Token

---

## 5. 圆角与阴影

### 5.1 圆角 Token

| Token | 值 | 用途 | 应用场景 |
|-------|---|------|---------|
| `--radius-none` | 0px | 直角 | 表格、分割线 |
| `--radius-2xs` | 2px | 微圆角 | 小标签、徽章 |
| `--radius-xs` | 3px | 小圆角 | 紧凑按钮、输入框 |
| `--radius-sm` | 4px | 标准小圆角 | 按钮、输入框、下拉框 |
| `--radius-md` | 6px | 中圆角 | 卡片、面板 |
| `--radius-lg` | 8px | 大圆角 | 大卡片、模态框 |
| `--radius-xl` | 12px | 超大圆角 | 浮动面板、弹出层 |
| `--radius-2xl` | 16px | 巨大圆角 | 全屏模态框 |
| `--radius-full` | 9999px | 全圆 | 头像、圆形按钮、药丸标签 |

**规则**：
- 同一页面圆角层级不超过 3 种
- 嵌套元素外层圆角 ≥ 内层圆角
- 禁止使用非 Token 圆角值（如 5px、7px、9px、10px、15px 等）

### 5.2 阴影 Token

| Token | 用途 | 应用场景 |
|-------|------|---------|
| `--shadow-xs` | 微阴影 | 列表项分隔 |
| `--shadow-sm` | 小阴影 | 卡片默认态 |
| `--shadow-md` | 中阴影 | 卡片悬停态、下拉面板 |
| `--shadow-lg` | 大阴影 | 模态框、浮动面板 |
| `--shadow-xl` | 超大阴影 | 全屏弹窗 |
| `--shadow-glow` | 主色辉光 | 选中卡片、焦点元素 |
| `--shadow-glow-strong` | 强辉光 | 大屏高亮指标 |

**规则**：
- 阴影必须使用 Token，禁止硬编码 `box-shadow` 值
- 悬停态阴影比默认态高一级（如 sm → md）
- 辉光阴影仅用于需要视觉突出的元素

---

## 6. 页面布局

### 6.1 全局布局结构

```
┌─────────────────────────────────────────────────┐
│ Header (--header-height: 56px)                  │
├──────────┬──────────────────────────────────────┤
│ Sidebar  │ Content Area                         │
│ 240px    │ max-width: 1440px                    │
│ (64px    │ padding: var(--spacing-2xl)          │
│  折叠)   │                                      │
│          │                                      │
└──────────┴──────────────────────────────────────┘
```

### 6.2 布局 Token

| Token | 值 | 说明 |
|-------|---|------|
| `--sidebar-width` | 240px | 侧边栏展开宽度 |
| `--sidebar-collapsed` | 64px | 侧边栏折叠宽度 |
| `--sidebar-transition` | 0.3s ease | 侧边栏过渡动画 |
| `--header-height` | 56px | 顶栏高度 |
| `--content-max-width` | 1440px | 内容区最大宽度 |
| `--content-padding` | 24px | 内容区内边距 |

### 6.3 栅格系统

采用 CSS Grid + Flexbox 混合布局：

| 场景 | 布局方式 | 说明 |
|------|---------|------|
| 大屏仪表盘 | CSS Grid | `grid-template-columns: repeat(auto-fit, minmax(300px, 1fr))` |
| 表单页面 | Flexbox | 纵向堆叠，标签-输入框水平排列 |
| 列表页面 | Flexbox | 纵向堆叠，行内水平排列 |
| 卡片网格 | CSS Grid | 响应式自动填充 |

### 6.4 卡片布局

| Token | 值 | 说明 |
|-------|---|------|
| `--card-padding` | 16px | 卡片内边距 |
| `--card-gap` | 16px | 卡片间距 |
| `--card-min-height` | 120px | 卡片最小高度 |

**卡片规范**：
- 所有卡片必须使用 `var(--color-bg-elevated)` 背景
- 卡片边框使用 `1px solid var(--color-border-light)`
- 卡片圆角使用 `var(--radius-lg)`（8px）
- 卡片悬停时阴影升级一级（sm → md）

---

## 7. 组件样式

### 7.1 按钮规范

| 类型 | 背景 | 文字色 | 圆角 | 内距 |
|------|------|--------|------|------|
| 主按钮 | `var(--color-primary)` | `var(--color-white)` | `var(--radius-sm)` | `var(--spacing-sm) var(--spacing-lg)` |
| 次按钮 | `transparent` | `var(--color-primary)` | `var(--radius-sm)` | `var(--spacing-sm) var(--spacing-lg)` |
| 幽灵按钮 | `transparent` | `var(--color-text-secondary)` | `var(--radius-sm)` | `var(--spacing-xs) var(--spacing-md)` |
| 危险按钮 | `var(--color-error)` | `var(--color-white)` | `var(--radius-sm)` | `var(--spacing-sm) var(--spacing-lg)` |
| 图标按钮 | `transparent` | `var(--color-text-secondary)` | `var(--radius-sm)` | `var(--spacing-xs)` |

**按钮状态**：
- 悬停：背景亮度 +5%，添加 `var(--shadow-sm)`
- 按下：背景亮度 -5%，内距微调
- 禁用：`opacity: 0.5`，`cursor: not-allowed`
- 加载：显示旋转图标，文字保留

### 7.2 输入框规范

| 属性 | 值 |
|------|---|
| 高度 | 36px |
| 内距 | `var(--spacing-sm) var(--spacing-md)` |
| 圆角 | `var(--radius-sm)` |
| 边框 | `1px solid var(--color-border)` |
| 背景色 | `var(--color-bg-elevated)` |
| 文字色 | `var(--color-text-primary)` |
| 占位符色 | `var(--color-text-tertiary)` |
| 焦点边框 | `1px solid var(--color-border-focus)` |
| 焦点阴影 | `var(--shadow-glow)` |
| 错误边框 | `1px solid var(--color-error)` |

### 7.3 表格规范

| 属性 | 值 |
|------|---|
| 表头背景 | `var(--color-bg-elevated)` |
| 表头文字 | `var(--font-weight-semibold) var(--font-size-sm)` |
| 行高 | 44px |
| 行悬停 | `var(--color-bg-hover)` |
| 行选中 | `rgba(var(--color-primary-rgb), 0.08)` |
| 边框 | `1px solid var(--color-border-light)` |
| 单元格内距 | `var(--spacing-md) var(--spacing-lg)` |

### 7.4 标签/徽章规范

| 类型 | 圆角 | 内距 | 字号 |
|------|------|------|------|
| 状态标签 | `var(--radius-full)` | `var(--spacing-3xs) var(--spacing-sm)` | `var(--font-size-2xs)` |
| 计数徽章 | `var(--radius-full)` | `var(--spacing-3xs) var(--spacing-xs)` | `var(--font-size-2xs)` |
| 分类标签 | `var(--radius-sm)` | `var(--spacing-xs) var(--spacing-sm)` | `var(--font-size-xs)` |

### 7.5 模态框/弹窗规范

| 属性 | 值 |
|------|---|
| 遮罩 | `rgba(var(--color-black-rgb), 0.6)` |
| 背景 | `var(--color-bg-overlay)` |
| 圆角 | `var(--radius-xl)` |
| 阴影 | `var(--shadow-xl)` |
| 内距 | `var(--spacing-2xl)` |
| 最大宽度 | 520px（标准）/ 720px（宽） |

---

## 8. 交互反馈

### 8.1 焦点状态

**强制**：所有可交互元素必须支持键盘焦点。

```css
/* 全局焦点样式（已在 tokens.css 中定义） */
:focus-visible {
  outline: 2px solid var(--color-primary);
  outline-offset: 2px;
  border-radius: var(--radius-sm);
}

/* 鼠标点击不显示焦点环 */
:focus:not(:focus-visible):not(:active) {
  outline: none;
}
```

**规则**：
- 禁止使用 `outline: none` 全局移除焦点环
- 自定义焦点样式必须使用 `:focus-visible` 伪类
- 焦点环颜色必须使用 `var(--color-primary)`
- 焦点环宽度 ≥ 2px，偏移 ≥ 2px

### 8.2 悬停状态

| 元素类型 | 悬停效果 | 过渡时长 |
|----------|---------|---------|
| 按钮 | 背景亮度 +5% + 阴影升级 | `var(--duration-fast)` |
| 卡片 | 阴影升级 + 微上移（translateY -2px） | `var(--duration-normal)` |
| 列表行 | 背景色变为 `var(--color-bg-hover)` | `var(--duration-fast)` |
| 链接 | 下划线出现 + 颜色变浅 | `var(--duration-fast)` |
| 图标 | 透明度变化或颜色变化 | `var(--duration-fast)` |

### 8.3 激活/按下状态

| 元素类型 | 按下效果 |
|----------|---------|
| 按钮 | 背景亮度 -5%，`transform: scale(0.98)` |
| 卡片 | 无位移，阴影降级 |
| 列表行 | 背景色变为 `var(--color-bg-active)` |

### 8.4 加载状态

| 场景 | 反馈方式 | 实现 |
|------|---------|------|
| 按钮加载 | 旋转图标 + 文字保留 | SVG 旋转动画 |
| 页面加载 | 骨架屏 | 渐变闪烁动画 |
| 数据加载 | 进度条/Spinner | CSS 动画 |
| 长操作 | 进度百分比 | 动态宽度 |

**规则**：
- 所有加载动画必须尊重 `prefers-reduced-motion`
- 加载动画时长使用 `var(--duration-slow)` 或更长
- 禁止使用 `loading...` 纯文字，必须搭配视觉指示器

### 8.5 过渡动画规范

| Token | 值 | 用途 |
|-------|---|------|
| `--duration-instant` | 100ms | 微交互（颜色变化） |
| `--duration-fast` | 150ms | 悬停、焦点 |
| `--duration-normal` | 250ms | 展开/收起、淡入淡出 |
| `--duration-slow` | 350ms | 页面过渡、模态框 |
| `--duration-slower` | 500ms | 复杂动画 |

| Token | 值 | 用途 |
|-------|---|------|
| `--ease-default` | cubic-bezier(0.4, 0, 0.2, 1) | 默认缓动 |
| `--ease-in` | cubic-bezier(0.4, 0, 1, 1) | 元素离开 |
| `--ease-out` | cubic-bezier(0, 0, 0.2, 1) | 元素进入 |
| `--ease-bounce` | cubic-bezier(0.34, 1.56, 0.64, 1) | 弹性效果 |

---

## 9. 响应式设计

### 9.1 断点系统

| 断点名称 | 宽度 | 设备类型 | 布局策略 |
|----------|------|---------|---------|
| sm | ≤ 640px | 小手机 | 单列，侧边栏隐藏 |
| md | ≤ 768px | 平板竖屏 | 单列/双列自适应 |
| lg | ≤ 1024px | 平板横屏/小桌面 | 双列，侧边栏可折叠 |
| xl | ≤ 1280px | 标准桌面 | 多列，侧边栏展开 |
| 2xl | > 1280px | 大屏桌面 | 全功能布局 |

### 9.2 响应式适配规则

**侧边栏**：
```css
/* ≤ 768px：侧边栏隐藏，通过汉堡菜单触发 */
@media (max-width: 768px) {
  .sidebar { transform: translateX(-100%); }
  .sidebar.open { transform: translateX(0); }
}

/* 769px-1024px：侧边栏折叠 */
@media (min-width: 769px) and (max-width: 1024px) {
  .sidebar { width: var(--sidebar-collapsed); }
}

/* > 1024px：侧边栏展开 */
@media (min-width: 1025px) {
  .sidebar { width: var(--sidebar-width); }
}
```

**卡片网格**：
```css
/* 默认单列 */
.card-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: var(--card-gap);
}

/* ≥ 768px：双列 */
@media (min-width: 768px) {
  .card-grid { grid-template-columns: repeat(2, 1fr); }
}

/* ≥ 1280px：三列 */
@media (min-width: 1280px) {
  .card-grid { grid-template-columns: repeat(3, 1fr); }
}
```

**字号适配**：
```css
/* 移动端字号微调 */
@media (max-width: 768px) {
  :root {
    --font-size-4xl: 28px;  /* 原 32px */
    --font-size-5xl: 32px;  /* 原 36px */
    --content-padding: var(--spacing-md);
  }
}
```

### 9.3 大屏适配（OpsBigScreen 专用）

| 断点 | 布局策略 |
|------|---------|
| 1280px-1536px | 2 列指标卡 + 1 列图表 |
| 1536px-1920px | 3 列指标卡 + 2 列图表 |
| > 1920px | 4 列指标卡 + 3 列图表 |

---

## 10. 无障碍合规

### 10.1 焦点管理

| 要求 | 标准 | 验收方式 |
|------|------|---------|
| 所有可交互元素可通过 Tab 访问 | WCAG 2.1 AA 2.1.1 | 键盘遍历所有交互元素 |
| 焦点环可见 | WCAG 2.1 AA 2.4.7 | `:focus-visible` 样式生效 |
| 焦点顺序符合逻辑 | WCAG 2.1 AA 2.4.3 | Tab 顺序与视觉顺序一致 |
| 模态框焦点陷阱 | WCAG 2.1 AA 2.4.3 | 模态框内 Tab 循环 |

### 10.2 高对比度适配

```css
/* 已在 tokens.css 中定义 */
@media (prefers-contrast: more) {
  :root {
    --color-text-primary:   oklch(1 0 0);           /* 纯白 */
    --color-text-secondary: oklch(0.90 0.01 264);   /* 更亮 */
    --color-border:         oklch(0.50 0.02 264);   /* 更明显 */
  }
  :focus-visible {
    outline-width: 3px;    /* 更粗焦点环 */
    outline-offset: 3px;
  }
}
```

**规则**：
- 所有页面必须适配 `prefers-contrast: more`
- 高对比度模式下文字与背景对比度 ≥ 7:1
- 高对比度模式下边框必须可见

### 10.3 减少动画适配

```css
/* 已在 tokens.css 中定义 */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

**规则**：
- 所有页面必须适配 `prefers-reduced-motion: reduce`
- 减少动画模式下仅保留必要的状态变化（如颜色变化），移除位移/缩放/旋转动画
- 不可用 `!important` 覆盖减少动画媒体查询

### 10.4 色彩对比度

| 文字类型 | 最低对比度（AA） | 推荐对比度（AAA） |
|----------|-----------------|------------------|
| 正文文字（< 18px） | 4.5:1 | 7:1 |
| 大号文字（≥ 18px 或 ≥ 14px 加粗） | 3:1 | 4.5:1 |
| 图标/图形元素 | 3:1 | 4.5:1 |

---

## 11. 验收标准

### 11.1 代码级验收（CI 自动检查）

| 检查项 | 规则 | 合格标准 |
|--------|------|---------|
| 硬编码颜色 | 禁止 `#[0-9a-fA-F]{3,8}`、`rgb()`、`hsl()`、`rgba()` 硬编码 | 0 处违规 |
| 硬编码间距 | 禁止 `padding/margin/gap: \d+px` 非 Token 值 | 0 处违规 |
| 硬编码圆角 | 禁止 `border-radius: \d+px` 非 Token 值 | 0 处违规 |
| 硬编码字重 | 禁止 `font-weight: \d{3}` 非 Token 值 | 0 处违规 |
| 硬编码字号 | 禁止 `font-size: \d+px` 非 Token 值 | 0 处违规 |
| OKLCH 合规 | 所有颜色值使用 OKLCH 或 Token | 100% |
| :focus-visible | 所有交互组件有焦点样式 | 100% |
| prefers-contrast | 所有页面适配高对比度 | 100% |
| prefers-reduced-motion | 所有页面适配减少动画 | 100% |
| 768px 断点 | 所有页面有移动端适配 | 100% |

### 11.2 视觉级验收（人工检查）

| 检查项 | 合格标准 |
|--------|---------|
| 色彩一致性 | 同一语义用途颜色统一 |
| 间距节奏 | 间距使用 Token 值，视觉节奏均匀 |
| 字号层级 | 每页不超过 4 级字号 |
| 圆角统一 | 同类组件圆角一致 |
| 阴影层级 | 悬停态阴影比默认态高一级 |
| 焦点可见 | Tab 遍历所有交互元素可见焦点环 |
| 响应式 | 768px/1024px/1280px 三个断点布局正常 |

### 11.3 无障碍验收

| 检查项 | 工具 | 合格标准 |
|--------|------|---------|
| 键盘导航 | 手动 Tab 遍历 | 所有交互元素可访问 |
| 对比度 | Chrome DevTools / axe | AA 标准 100% |
| 屏幕阅读器 | NVDA/VoiceOver | 关键信息可读 |
| 高对比度 | Windows 高对比度模式 | 页面可用 |
| 减少动画 | 系统偏好设置 | 动画禁用，功能正常 |

---

## 12. 附录：硬编码迁移映射表

### 12.1 颜色迁移映射

| 硬编码值 | Token 替换 | 说明 |
|----------|-----------|------|
| `#165DFF` / `rgba(22,93,255,...)` | `var(--color-primary)` / `rgba(var(--color-primary-rgb), α)` | 品牌主色 |
| `#5E9DFF` / `rgba(94,157,255,...)` | `var(--color-primary-light)` / `rgba(var(--color-primary-light-rgb), α)` | 主色浅色 |
| `#0A84FF` / `rgba(10,132,255,...)` | `var(--color-cosmic)` / `rgba(var(--color-cosmic-rgb), α)` | 宇宙蓝 |
| `#EF4444` / `rgba(239,68,68,...)` | `var(--color-error)` / `rgba(var(--color-error-rgb), α)` | 错误色 |
| `#F59E0B` / `rgba(245,158,11,...)` | `var(--color-warning)` / `rgba(var(--color-warning-rgb), α)` | 警告色 |
| `#3B82F6` / `rgba(59,130,246,...)` | `var(--color-info)` / `rgba(var(--color-info-rgb), α)` | 信息色 |
| `#22C55E` | `var(--color-success)` | 成功色 |
| `#fff` / `#FFFFFF` / `rgba(255,255,255,...)` | `var(--color-white)` / `rgba(var(--color-white-rgb), α)` | 白色 |
| `#000` / `rgba(0,0,0,...)` | `var(--color-black)` / `rgba(var(--color-black-rgb), α)` | 黑色 |
| `#0F172A` / `rgba(15,23,42,...)` | `var(--color-bg-deep)` / `rgba(var(--color-bg-deep-rgb), α)` | 深层背景 |

### 12.2 间距迁移映射

| 硬编码值 | Token 替换 |
|----------|-----------|
| `2px` | `var(--spacing-3xs)` |
| `4px` | `var(--spacing-2xs)` |
| `6px` | `var(--spacing-xs)` |
| `8px` | `var(--spacing-sm)` |
| `10px` | `var(--spacing-sm-md)` |
| `12px` | `var(--spacing-md)` |
| `14px` | `var(--spacing-md-lg)` |
| `16px` | `var(--spacing-lg)` |
| `20px` | `var(--spacing-xl)` |
| `24px` | `var(--spacing-2xl)` |
| `32px` | `var(--spacing-3xl)` |
| `40px` | `var(--spacing-4xl)` |
| `48px` | `var(--spacing-5xl)` |

### 12.3 圆角迁移映射

| 硬编码值 | Token 替换 |
|----------|-----------|
| `0px` | `var(--radius-none)` |
| `2px` | `var(--radius-2xs)` |
| `3px` | `var(--radius-xs)` |
| `4px` | `var(--radius-sm)` |
| `6px` | `var(--radius-md)` |
| `8px` | `var(--radius-lg)` |
| `12px` | `var(--radius-xl)` |
| `16px` | `var(--radius-2xl)` |
| `9999px` / `50%` | `var(--radius-full)` |

### 12.4 字重迁移映射

| 硬编码值 | Token 替换 |
|----------|-----------|
| `400` / `normal` | `var(--font-weight-regular)` |
| `500` | `var(--font-weight-medium)` |
| `600` | `var(--font-weight-semibold)` |
| `700` / `bold` | `var(--font-weight-bold)` |
| `800` | `var(--font-weight-extrabold)` |

### 12.5 字号迁移映射

| 硬编码值 | Token 替换 |
|----------|-----------|
| `8px` / `9px` / `10px` | `var(--font-size-2xs)` |
| `11px` | `var(--font-size-xs)` |
| `12px` / `13px` | `var(--font-size-sm)` |
| `14px` | `var(--font-size-base)` |
| `15px` | `var(--font-size-md)` |
| `16px` | `var(--font-size-lg)` |
| `18px` | `var(--font-size-xl)` |
| `20px` | `var(--font-size-2xl)` |
| `22px` / `24px` | `var(--font-size-3xl)` |
| `32px` | `var(--font-size-4xl)` |
| `36px` | `var(--font-size-5xl)` |
| `48px` | `var(--font-size-6xl)` |

### 12.6 字体族迁移映射

| 硬编码值 | Token 替换 |
|----------|-----------|
| `monospace` / `"JetBrains Mono", monospace` / `"Fira Code", monospace` | `var(--font-family-mono)` |
| `sans-serif` / `system-ui` | `var(--font-family-base)` |

---

> **规范维护**：本规范随 `tokens.css` 同步更新。任何 Token 新增/修改/废弃需同步更新本文档。
> **合规基线**：当前审计约 1202 处违规，目标合规率 100%。修复优先级按审计报告重灾区文件排序。
